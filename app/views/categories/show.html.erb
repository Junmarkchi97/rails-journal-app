<p style="color: green"><%= notice %></p>

<%= render @category %>

<div class="show">
  <%= link_to "Edit this category", edit_category_path(@category) , class:"btn btn-primary"%>
  <%= link_to "Back to categories", categories_path, class:"btn btn-secondary" %>
  <%= button_to "Destroy this category", @category, method: :delete, class:"btn btn-danger" %>
</div>
<%= link_to "View Tasks", category_tasks_path(@category), class:"btn btn-success" %>
